services:
  ai-khemra-bro:
    build: .
    container_name: ai-khemra-bro
    restart: unless-stopped
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ai_khemra_data:/data

volumes:
  ai_khemra_data:
